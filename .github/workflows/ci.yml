# .github/workflows/ci.yml
name: CloudStrike CI

on:
  push:
    branches:
      - main
      - "**"
  pull_request:
    branches:
      - main

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
      # Step 1: Checkout repository
      - name: Checkout repository
        uses: actions/checkout@v3

      # Step 2: Setup Python 3.14.1
      - name: Set up Python 3.14.1
        uses: actions/setup-python@v5
        with:
          python-version: '3.14.1'

      # Step 3: Install dependencies
      - name: Install dependencies
        run: |
          python3 -m pip install --upgrade pip
          python3 -m pip install --upgrade --force-reinstall flake8 pytest
          if [ -f "requirements.txt" ]; then
            python3 -m pip install -r requirements.txt
          fi

      # Step 4: Lint code with flake8
      - name: Lint with flake8
        run: |
          python3 -m flake8 cloudstrike

      # Step 5: Run tests if tests directory exists
      - name: Run tests
        run: |
          if [ -d "tests" ]; then
            python3 -m pytest tests
          else
            echo "No tests directory found, skipping tests."
          fi
